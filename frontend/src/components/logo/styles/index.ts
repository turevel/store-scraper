import styled from 'styled-components';

export const Logo = styled.h1`
  align-items: center;
  column-gap: 10px;
  display: flex;
  font-size: 1.7rem;
  top: 15px;
  left: 15px;
  position: absolute;
  transition: all ease-in-out 200ms;
`;
